<!-- GENERATED by scripts/generate_docs_map.py — do not edit by hand. -->

# Documentation map

Every heading in `docs/` and `skills/`, so a contributor (or a tool) can find the right
section without opening ten files. Regenerate with `python scripts/generate_docs_map.py`.


## `docs/AI_COLLABORATION.md`

> The tool-neutral working contract for humans and AI assistants contributing to Nodera — minimum capabilities a tool must meet, entry points per role, the handoff format, and how to attach a new tool.

- AI collaboration — one project, free choice of tool
  - 1. The shared minimum contract
  - 2. Entry point by role
  - 3. Supported entry points
  - 4. The independent review, concretely
  - 5. Cross-role handoff
  - 6. Working with agents as contributors
  - 7. Maintenance boundary

## `docs/API_CONTRACT.md`

> The REST contract — resource shapes, the actor envelope every response carries, error bodies, pagination, idempotency, and the rule that the OpenAPI document is written before the routes rather than derived from them.

- REST API contract — Nodera
  - 1. Conventions
  - 2. The actor envelope
  - 2a. Health — the only implemented endpoints today
  - 3. Resources
    - Projects
    - Tickets
    - Acceptance criteria, dependencies, labels
    - Comments and reviews
    - Actors, membership, credentials, audit
  - 4. Errors
  - 5. Pagination
  - 6. Idempotency
  - 7. What the API layer must never do

## `docs/ARCHITECTURE.md`

> How Nodera is built — hexagonal Kotlin backend with REST and MCP as two adapters over one domain core, React/TypeScript frontend, PostgreSQL with row-level security, and the reasoning behind each choice.

- Architecture — Nodera
  - 1. The shape in one picture
  - 2. Backend — Kotlin, hexagonal, six modules
    - 2.1 Chosen libraries, and what each is doing there
    - 2.2 What must never happen in an adapter
  - 3. Frontend — React, mobile-first
  - 4. Database — PostgreSQL 16+
  - 5. Identity and access
  - 6. The MCP server
  - 7. Deployment
  - 8. Decisions recorded elsewhere

## `docs/DOMAIN_MODEL.md`

> The Nodera domain model — actor (human or agent), project, ticket, assignment, comment, review, capability grant and audit event — with the invariants each entity carries and the reasoning behind the shape.

- Domain model — Nodera
  - 1. Entity map
  - 2. `actor` — the root of the model
    - 2.1 `human_actor`
    - 2.2 `agent_actor`
  - 3. `project` — the multi-project boundary
  - 4. Permissions — roles, capabilities and attenuation
  - 5. `ticket`
    - 5.1 Status and transitions
    - 5.2 `acceptance_criterion`
    - 5.3 `ticket_dependency`
  - 6. `comment`
  - 7. `review` and `review_finding`
  - 8. `credential`
  - 9. `audit_event` — append-only
  - 10. Markdown interchange

## `docs/INDEX.md`

> The documentation hub — the single entry point that routes every contributor, human or agent, to the right source of truth for the work in front of them.

- Documentation index — Nodera
  - Session start
  - The two-layer architecture
  - Product and scope
  - Process
  - Skills — loaded on demand, not permanently in context
  - Mandatory-reading rules
  - Maintenance

## `docs/MCP.md`

> Nodera's Model Context Protocol server — the complete tool and resource surface, the capability each tool requires, authentication, pagination, idempotency and the rules that keep MCP from becoming a second, weaker API.

- MCP server — Nodera
  - 1. The four rules
  - 2. Connecting
    - Transports
    - Authentication
    - Client configuration
  - 3. Tool surface
    - 3.1 Orientation
    - 3.2 Reading work
    - 3.3 Changing work
    - 3.4 Deliberately absent
  - 4. The closure gate, as an agent experiences it
  - 5. Idempotency
  - 6. Pagination
  - 7. Resources
  - 8. Prompts
  - 9. Errors
  - 10. Rate limits

## `docs/PROJECT_MANAGEMENT.md`

> How work is organised in this repository — the ticket system, the session protocol, priorities, the closure protocol, ticket hygiene and the repository language rule.

- Project management — Nodera
  - 1. The ticket system, and why it is Markdown
  - 2. Session start protocol
  - 3. The five-phase workflow
    - What "independent" means here
  - 4. Repository language
  - 5. Priorities
  - 6. ID prefixes
  - 7. Registering a new work package
  - 8. Ticket hygiene — when a review finding becomes a ticket
    - The primary rule: scope the relief to the reasoning
    - The ticket test, for what is left over
  - 9. The closure protocol
  - 10. Plans
  - 11. Retrospectives

## `docs/VISION.md`

> What Nodera is, the one premise it is built on (humans and AI agents are the same kind of participant), the scope fence of what will and will not be built, and how the project judges whether it succeeded.

- Vision — Nodera
  - 1. The premise
  - 2. What Nodera is
  - 3. What Nodera is deliberately not
  - 4. The five capabilities that define "first-class"
  - 5. The reference workflow Nodera must serve
  - 6. Success criteria
  - 7. Non-goals for version 1.0

## `docs/adr/0001-actor-not-user.md`

- ADR-0001 — Actor, not user: one participant type for humans and agents
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/0002-provider-agnostic-agent-adapters.md`

- ADR-0002 — Provider-agnostic agent adapters: a two-layer architecture for AI tooling
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/0003-ticket-frontmatter-source-of-truth.md`

- ADR-0003 — Ticket frontmatter is the source of truth; every table view is generated
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/0004-markdown-tickets-until-self-hosting.md`

- ADR-0004 — Markdown tickets until Nodera can host its own backlog
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/0005-mcp-as-sibling-surface.md`

- ADR-0005 — MCP is a sibling surface, not a wrapper over REST
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/0006-one-image-three-entrypoints.md`

- ADR-0006 — One image, three entrypoints; migrations are a separate step
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/0007-deployment-is-the-tenant-boundary.md`

- ADR-0007 — The deployment is the tenant boundary
  - Context
  - Decision
  - Consequences
  - Alternatives considered

## `docs/adr/README.md`

- Architecture decision records
  - When to write one
  - Format
  - Records

## `docs/ci.md`

> Every CI job, what it enforces, and the exact local command that reproduces it — so a red pipeline is always reproducible on a laptop and never requires a guess-and-push cycle.

- CI — jobs and their local equivalents
  - Job-to-local mapping
  - Repository checks, step by step
  - The database lane runs the same migrator the image runs
  - Why the database lane runs the migrations twice
  - Why the frontend lane regenerates the API client
  - The image is verified separately
  - What CI does not check
  - Adding a lane

## `docs/plan/OPS-01.md`

- Plan — OPS-01 · Build chain and release package
  - 1. What phase 1 found, and how it differs from the ticket
  - 2. The rule this plan refuses to break
  - 3. Files to change, and why each one
    - 3.1 Unblock the toolchain
    - 3.2 The three entrypoints (ADR-0006)
    - 3.3 Health, and the contract it owes
    - 3.4 The minimum buildable frontend
    - 3.5 Image and release
  - 4. Decisions taken in this plan
  - 5. Test plan
  - 6. Deliberate non-goals
  - 7. What this package cannot make green, stated rather than worked around
  - 8. Open questions
  - 9. Added or corrected during implementation
  - 10. Answers to § 8

## `docs/plan/README.md`

- Plans
  - Why they persist
  - Status header
  - Shape
  - Current plans

## `docs/prompts/README.md`

- Prompts
  - Catalogue
  - Adding one

## `docs/prompts/code-review.prompt.md`

- Canonical review prompt — Nodera phase 4

## `docs/prompts/maintain-adapters.prompt.md`

- Adapter maintenance prompt

## `skills/README.md`

> Catalogue of Nodera's skills — what each one governs and when to load it. Skills are loaded on demand, never permanently in context.

- Skills — Nodera
  - Catalogue
  - Adding a skill

## `skills/agent-actors.md`

> How to build features that treat humans and AI agents as the same kind of participant — the modelling rules, the permission and delegation mechanics, what agent-specific handling is legitimate, and the failure patterns that quietly demote agents back to bot accounts.

- Agent actors — building for two kinds of participant
  - 1. The rule, and the one question that enforces it
  - 2. Legitimate agent-specific handling
  - 3. The delegation chain
  - 4. Attenuation, concretely
  - 5. Assignment
  - 6. Comments
  - 7. Failure patterns — what this looks like when it goes wrong
  - 8. Reviewing this area

## `skills/backend-kotlin.md`

> Kotlin backend conventions — module boundaries and what the build enforces, ActorContext as a parameter, error modelling, transactions, and the patterns that keep adapters thin.

- Backend (Kotlin) — conventions
  - Module boundaries
  - ActorContext is a parameter, never ambient
  - Errors are values in the domain, exceptions at the edge
  - Transactions
  - Nullability and value types
  - Concurrency
  - Style gates

## `skills/code-review.md`

> The phase-4 review rubric — what an independent reviewer checks, how findings are classified BLOCKING or NON-BLOCKING, the evidence standard a finding must meet, and the scope governor that keeps a review from becoming a rewrite.

- Code review — Nodera
  - 1. What a review returns
  - 2. Classification
  - 3. The evidence standard
  - 4. The review checklist, in order
  - 5. The scope governor
  - 6. Re-review after fixes
  - 7. Recording the result

## `skills/critical-invariants.md`

> The twelve hard invariants of Nodera — actor kind never gates permission, one permission engine, append-only audit, attenuation at use time, server-side scoping, forward-only migrations, and the rest. A violation is a BLOCKING finding, not a discussion.

- Critical invariants — Nodera
  - 1. Actor kind never gates permission
  - 2. One permission engine
  - 3. The audit trail is append-only, and complete
  - 4. Attenuation is re-checked at use time
  - 5. Scoping is server-side, and RLS is the floor
  - 6. Credentials fail closed and are never readable
  - 7. Migrations are forward-only
  - 8. The closure gate is not clickable
  - 9. The reviewer is not the author
  - 10. Ticket keys are permanent
  - 11. Contract-first, generated types
  - 12. Entry files and this hub change only inside a work package
  - When one of these seems to be in the way

## `skills/database-design.md`

> Database conventions — forward-only migrations, identifier casing, RLS on every project-scoped table, indexing rules, and how an invariant becomes a constraint rather than a comment.

- Database design — Nodera
  - Migrations are forward-only
  - Identifiers
  - Put the invariant in the database when the database can hold it
  - Row-level security
  - Indexing
  - Types
  - What does not belong in the database

## `skills/frontend-react.md`

> React and TypeScript conventions — mobile-first layout, the generated API layer, rendering actor kind, component testing, and the accessibility floor every view meets.

- Frontend (React) — conventions
  - Mobile-first is a constraint, not a preference
  - Never call `fetch` in a component
  - Render `actor.kind` — never infer it
  - Every unit ships with a test
  - Accessibility floor
  - State
  - Gates

## `skills/mcp-integration.md`

> Rules for the MCP server — how a tool is defined, the capability it declares, argument validation, idempotency, error shape, and the parity requirement that keeps MCP from becoming a weaker API.

- MCP integration — conventions
  - A tool is a thin translation, and nothing else
  - Parity is tested, not asserted
  - Declaring capability
  - Argument validation
  - Idempotency
  - Errors carry structure
  - Audit every call
  - Pagination
  - What must not be added

## `skills/secure-coding.md`

> Security rules — fail closed, credential handling, the permission check placement, input validation at the boundary, what may never be logged, and the paired-negative test requirement for every safety claim.

- Secure coding — Nodera
  - Fail closed, everywhere
  - Credentials
  - Where the permission check goes
  - Input validation at the boundary
  - Scoping
  - Every safety claim ships with a paired-negative test
  - Rate limiting
  - Dependencies

## `skills/testing.md`

> What to test and how — the paired-negative rule, real Postgres for anything involving RLS, surface parity tests, per-file coverage, and the tests that are worse than no test.

- Testing — Nodera
  - The paired-negative rule
  - Real Postgres for anything the database enforces
  - Surface parity
  - Coverage
  - Tests that are worse than no test
  - Naming
  - What does not need a test
