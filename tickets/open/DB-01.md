---
id: DB-01
title: Apply the baseline schema and prove row-level security with negative tests
priority: P1
status: open
effort: ~2 d
depends_on: []
created: 2026-08-20
updated: 2026-08-20
---

# DB-01 · Apply the baseline schema and prove row-level security with negative tests

**Priority:** P1
**Effort:** ~2 d

## Motivation / context

The four migrations exist but have never been applied to a database. Until they are, every
invariant they claim to enforce — the append-only audit grant, the RLS policies, the ownership
chain trigger — is a claim in a file. A schema that has not run is not a schema.

## Current state (honest)

`V1` to `V4` exist and are internally consistent by reading. Flyway is configured in the Gradle
build but has never been executed. No Testcontainers harness exists. The `nodera_app_password`
placeholder in V4 has no substitution wired up.

## Approach

1. Wire the Flyway placeholders and run `flywayMigrate` against the compose Postgres.
2. Build the Testcontainers harness in `:persistence` — a real Postgres 16, migrations applied once
   per test class.
3. Write one negative test per enforced rule: RLS on each project-scoped table, the append-only
   audit triggers, actor-kind immutability, the agent-ownership chain, the dependency-cycle trigger
   and reviewer independence.
4. Add `scripts/lint_sql.py`: unquoted lowercase identifiers, every foreign key indexed.

## Acceptance criteria

- [ ] `./gradlew flywayMigrate` applies all four migrations to an empty database with no error.
- [ ] Every RLS policy has a test that is demonstrably red when the policy is dropped; both outcomes
      are recorded in the review result.
- [ ] `nodera_app` cannot update, delete or truncate `audit_event` — proved by a test asserting the
      failure, not by reading the grant.
- [ ] A cross-project read returns **zero rows** rather than an error, confirming the polarity is
      fail-closed rather than loud-but-permissive.
- [ ] `scripts/lint_sql.py` passes, and fails on a deliberately quoted mixed-case identifier.
- [ ] `make check` green.
- [ ] Independent review: 0 BLOCKING findings.

## Affected files

- `db/migrations/` — placeholder wiring only; no edit to an applied migration.
- `backend/persistence/src/test/kotlin/.../SchemaInvariantsTest.kt` — the negative tests.
- `scripts/lint_sql.py` — new.

## Verification

`./gradlew :persistence:test` with Docker running. Each negative test is run twice — with the guard
in place and with it removed — and both outcomes go in the review result.
